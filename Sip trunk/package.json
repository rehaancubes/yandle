{
  "name": "voxa-sip-trunk",
  "version": "1.0.0",
  "description": "SIP trunk bridge: HTTP call-start + TCP AudioSocket → Voxa Sonic (Socket.IO)",
  "main": "transcriber.js",
  "scripts": {
    "start": "node transcriber.js",
    "test": "node -e \"require('./db').getOrgByDid('+15551234567').then(console.log)\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "express": "^4.21.0",
    "socket.io-client": "^4.8.0"
  }
}
